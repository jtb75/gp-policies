name: Outpost Lite - Wiz Helm Deployment

on:
  workflow_dispatch:
    inputs:
      action:
        description: 'Action to perform'
        required: true
        type: choice
        options:
          - install
          - upgrade
          - uninstall

permissions:
  id-token: write
  contents: read

jobs:
  deploy:
    name: ${{ inputs.action }}
    runs-on: ubuntu-latest
    env:
      CLUSTER_NAME: jtb75-wiz-remediation
      NAMESPACE: wiz-remediation

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
          aws-region: us-east-1

      - name: Configure kubectl
        run: aws eks update-kubeconfig --name $CLUSTER_NAME --region us-east-1

      - name: Create namespace
        if: inputs.action != 'uninstall'
        run: kubectl create namespace $NAMESPACE --dry-run=client -o yaml | kubectl apply -f -

      - name: Create image pull secret
        if: inputs.action != 'uninstall'
        run: |
          kubectl create secret docker-registry wiz-outpost-lite-image-pull \
            --docker-server=wizio.azurecr.io \
            --docker-username="${{ secrets.WIZ_DOCKER_USERNAME }}" \
            --docker-password="${{ secrets.WIZ_DOCKER_PASSWORD }}" \
            --docker-email="" \
            -n $NAMESPACE \
            --dry-run=client -o yaml | kubectl apply -f -

      - name: Generate values.yaml
        if: inputs.action != 'uninstall'
        env:
          WIZ_OUTPOST_CLIENT_ID: ${{ secrets.WIZ_OUTPOST_CLIENT_ID }}
          WIZ_OUTPOST_CLIENT_SECRET: ${{ secrets.WIZ_OUTPOST_CLIENT_SECRET }}
          WIZ_OUTPOST_ID: ${{ secrets.WIZ_OUTPOST_ID }}
        run: |
          envsubst < remediation-infra/outpost-lite-values.tpl.yaml > values.yaml
          echo "--- Generated values.yaml (first 10 lines) ---"
          head -10 values.yaml

      - name: Add Wiz Helm repo
        if: inputs.action != 'uninstall'
        run: |
          helm repo add wiz-sec https://charts.wiz.io
          helm repo update

      - name: Install / Upgrade Outpost Lite
        if: inputs.action != 'uninstall'
        run: |
          helm upgrade outpost-lite wiz-sec/wiz-outpost-lite \
            --install \
            --values=values.yaml \
            -n $NAMESPACE \
            --wait \
            --timeout 10m

      - name: Uninstall Outpost Lite
        if: inputs.action == 'uninstall'
        run: |
          helm uninstall outpost-lite -n $NAMESPACE || true

      - name: Verify deployment
        if: inputs.action != 'uninstall'
        run: |
          kubectl get pods -n $NAMESPACE
          echo ""
          helm list -n $NAMESPACE
